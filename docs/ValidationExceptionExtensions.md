# ValidationExceptionExtensions

Provides extension methods for `ValidationException` to simplify error handling and aggregation of validation failures. These methods enable fluent construction of validation exceptions, aggregation of multiple error messages, and querying of specific validation errors.

## API

### `WithError`
